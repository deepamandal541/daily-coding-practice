import java.io.*;
import java.util.*;

public class Main {
    static String [] patterns = {".;","abc","def","ghi","jkl","mno","pqrs","tu","vwx","yz"};

    static void printAllcomb (String str,String asf,int idx){

    if(idx == str.length()){
    System.out.println(asf);
    return;
    }

int num = str.charAt(idx)-'0';
String pattern  = patterns[num];
for(int i = 0; i < pattern.length();i++){
printAllcomb(str,asf + pattern.charAt(i),idx + 1 );
}
}

    static void printKPC(String ques) {
        //Writ
        printAllcomb(ques,"",0);
    }

    public static void main(String[] args) throws Exception {
        Scanner sc = new Scanner(System.in);
        String str;
        str = sc.nextLine();
        printKPC(str);
    }
}

//QUESTION

You are given a string str. The string str will contains numbers only, where each number stands for a key pressed on a mobile phone. The following list is the key to characters map

0 -> .;
1 -> abc 
2 -> def 
3 -> ghi 
4 -> jkl 
5 -> mno 
6 -> pqrs 
7 -> tu 
8 -> vwx 
9 -> yz 
Complete the body of printKPC function - without changing signature - to print the list of all words that could be produced by the keys in str.

Use sample input and output to take idea about output

Input Format
Input consists of one line.

First line contains a string str.

Output Format
Words that can be produced by pressed keys indictated by str in order hinted by Sample output

Example 1
Input

78
Output

tv
tw
tx
uv
uw
ux
Explanation

These are the strings that can be formed.

tv, tw, tx, uv, uw, ux

Example 2
Input

1
Output

a
b
c
Explanation

These are the strings that can be formed.

a, b, c

Constraints
0 <= str.length <= 10

str contains numbers only

Topics
Recursion
Strings
Backtracking
Companies
Accolite
Flipkart
Amazon
